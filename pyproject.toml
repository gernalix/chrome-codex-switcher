[project]
name = "chrome-codex-switcher"
version = "0.4.0"
description = "Pair Chrome tabs with Codex Desktop threads on Fedora/Wayland"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
context-twin = "chrome_codex_switcher.cli:main"
